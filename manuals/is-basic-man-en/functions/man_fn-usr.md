# USR(N,X)

Calls an address **N** (which will probably have been defined using [CODE](commands/man_cs-code.md)), and passes the integer **X** in **HL** to the machine code routine. The value left in **HL** will be the value returned by **USR**.
