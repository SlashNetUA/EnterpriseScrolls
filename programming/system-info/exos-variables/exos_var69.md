# 69 ECHO

Determines the "echo" state of IS-DOS batch files. If **ON** is specified (within a batch file or prior to executing it), each line in the file will be printed when implemented. The default is **OFF**.